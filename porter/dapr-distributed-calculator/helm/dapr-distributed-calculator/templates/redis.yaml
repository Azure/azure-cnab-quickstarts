apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.redis
  metadata:
  - name: redisHost
    value: '{{ .Values.redis.host }}'
  - name: redisPassword
    value: '{{ .Values.redis.password }}'